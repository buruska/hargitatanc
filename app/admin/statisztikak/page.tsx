import { AdminShell } from "../admin-shell";
import { adminTitle } from "@/lib/styles";

export default function AdminStatisztikakPage() {
  return (
    <AdminShell>
      <h1 className={adminTitle}>Statisztikák</h1>
    </AdminShell>
  );
}
